[gd_resource type="Resource" script_class="FloorConfig" load_steps=25 format=3]

[ext_resource type="Script" path="res://scripts/resources/floor_config.gd" id="1_floor"]
[ext_resource type="Resource" path="res://data/rooms/data_cold_aisle.tres" id="2_start"]
[ext_resource type="Resource" path="res://data/rooms/data_intake_row.tres" id="3_intake"]
[ext_resource type="Resource" path="res://data/rooms/data_hot_aisle.tres" id="4_hot_aisle"]
[ext_resource type="Resource" path="res://data/rooms/data_chiller_bank.tres" id="5_chiller"]
[ext_resource type="Resource" path="res://data/rooms/data_grid_floor.tres" id="6_grid"]
[ext_resource type="Resource" path="res://data/rooms/data_busway.tres" id="61_busway"]
[ext_resource type="Resource" path="res://data/rooms/data_hot_containment.tres" id="62_containment"]
[ext_resource type="Resource" path="res://data/rooms/data_tape_library.tres" id="63_tape"]
[ext_resource type="Resource" path="res://data/rooms/data_cache_vault.tres" id="7_treasure"]
[ext_resource type="Resource" path="res://data/rooms/shop_bay.tres" id="8_shop"]
[ext_resource type="Resource" path="res://data/rooms/data_core_arena.tres" id="9_boss_room"]
[ext_resource type="Resource" path="res://data/settings/shop_config.tres" id="10_shop_cfg"]
[ext_resource type="Resource" path="res://data/spawns/data_center/ticket_bot.tres" id="11_ticket_bot"]
[ext_resource type="Resource" path="res://data/spawns/data_center/firewall_node.tres" id="12_firewall_node"]
[ext_resource type="Resource" path="res://data/spawns/data_center/code_runner.tres" id="13_code_runner"]
[ext_resource type="Resource" path="res://data/spawns/data_center/null_pointer.tres" id="14_null_pointer"]
[ext_resource type="Resource" path="res://data/spawns/load_balancer.tres" id="15_load_balancer"]
[ext_resource type="Resource" path="res://data/spawns/stale_replica.tres" id="16_stale_replica"]
[ext_resource type="Resource" path="res://data/floors/data_center_theme.tres" id="40_theme"]
[ext_resource type="Resource" path="res://data/pools/run_item_pool.tres" id="91_pool"]
[ext_resource type="Resource" path="res://data/bosses/cascade_failure_encounter.tres" id="92_cascade"]
[ext_resource type="Resource" path="res://data/bosses/scrap_king_encounter.tres" id="93_king"]
[ext_resource type="Resource" path="res://data/bosses/runtime_error_encounter.tres" id="94_error"]
[ext_resource type="Resource" path="res://data/bosses/orchestrator_encounter.tres" id="95_orchestrator"]

[resource]
script = ExtResource("1_floor")
id = &"data_center"
display_name = "Data Center"
floor_number = 3
theme = ExtResource("40_theme")
floor_tags = Array[StringName]([&"data_center"])
room_count = 10
start_templates = Array[RoomTemplate]([ExtResource("2_start")])
; Seven combat templates against the four this floor shipped with, and the count is the point rather
; than the variety. With four, `data_intake_row` — the teaching room, whose two spawn points are both
; forced Ticket Bots and which therefore draws *nothing* from the roster — was 2.4 of the floor's six
; combat rooms, and the floor put 17 enemies on the ground against the 22 of both floors before it.
; The last floor of the campaign was the emptiest one.
;
; The three new rooms are all at difficulty 2 and 3, which is where a floor's rooms actually get
; drawn, and all three carry four or five spawn points. See each one for what it asks; between them
; they are the floor's answer to "the ground has a shape" now that `CableDuct` exists to give it one.
combat_templates = Array[RoomTemplate]([ExtResource("3_intake"), ExtResource("4_hot_aisle"), ExtResource("5_chiller"), ExtResource("6_grid"), ExtResource("61_busway"), ExtResource("62_containment"), ExtResource("63_tape")])
treasure_templates = Array[RoomTemplate]([ExtResource("7_treasure")])
shop_templates = Array[RoomTemplate]([ExtResource("8_shop")])
boss_templates = Array[RoomTemplate]([ExtResource("9_boss_room")])
; A curated roster, which is what this floor is for: six entries against Development's seven, and
; every one of them chosen because it asks a question about *where the player is standing*. Ticket
; Bot for baseline pressure and because the teaching room needs something mild at difficulty one.
; Firewall Node and Null Pointer because both already charge for holding a position, which is the
; habit the zones charge for — the floor and its roster say the same thing in two languages. Code
; Runner because a moving target is what makes standing still tempting in the first place. Then the
; two new ones.
;
; What is deliberately absent is as much of the point. Memory Leech, Recursion and Deadlock are all
; enemies whose answer is about *timing* — read the windup, choose the moment, break the tether —
; and a floor teaching a positional idea should not spend half its rooms asking a different
; question. Pop Up Drone is left out for the opposite reason: it teleports, so nothing the player
; learns about holding ground applies to it.
;
; **Four of the six entries are this floor's own `EnemySpawn` resources**, under `data/spawns/data_center/`,
; and that is a correction rather than a flourish. A shared spawn resource is a single weight that has
; to be right on every floor that lists it, and the numbers said it was not: the Ticket Bot arrived
; here on the Help Desk's weight of 2.0 and made up 7.6 of this floor's 17 enemies, while Load
; Balancer and Stale Replica — the two enemies this floor was built to introduce — came to 1.4 each
; and were missing from a fifth of floors outright. A player could finish the Data Center without
; meeting either of the things it exists to teach. The two floor-exclusive entries are simply
; reweighted in place; the four shared ones are copied so that reweighting them here cannot reweight
; them on floors 1 and 2.
enemy_spawns = Array[EnemySpawn]([ExtResource("11_ticket_bot"), ExtResource("12_firewall_node"), ExtResource("13_code_runner"), ExtResource("14_null_pointer"), ExtResource("15_load_balancer"), ExtResource("16_stale_replica")])
clear_scrap_range = Vector2i(1, 3)
enemy_scrap_range = Vector2i(0, 2)
item_pool = ExtResource("91_pool")
item_clear_indices = Array[int]([2, 5])
shop = ExtResource("10_shop_cfg")
treasure_grants_item = true
; All three, which reverses this floor's earlier decision to keep Cascade Failure to itself. That
; decision rested on one claim — that a player meeting the vents before nine rooms of the Data
; Center would be meeting an unexplained mechanic — and the claim does not survive looking at what
; the fight actually does. Every hazard it puts down is a `ThermalZone`, drawn cold and climbing
; through amber for 1.6 seconds before it costs anything, on ground nothing else is standing on.
; That is a telegraph a first-floor player reads on its own terms; the floor teaches the ramp
; faster, it does not teach it first. What the lock cost, meanwhile, was real and paid every run:
; the last fight of the campaign was the same fight every time.
;
; The other direction is the one this floor was always missing. The Scrap King and Runtime Error
; are answered with skills the game teaches in its first room, so nothing ever stopped them
; guarding this floor — they were simply never listed. They arrive here in a room the other two
; floors do not have: `data_core_arena` keeps its four corner zones, so the King's terminals now
; stand in ground that charges for standing still to shoot them, and Runtime Error's lanes sweep a
; floor with cold centre and hot corners. Same fights, this floor's question asked over the top.
;
; `CampaignValidator`'s boss supply stops being exactly tight — three bosses, three floors, any
; dealing works — which is the point of a pool. A fourth floor is now a fourth boss, not a
; re-plumbing of the first three.
boss_pool = Array[BossEncounter]([ExtResource("92_cascade"), ExtResource("93_king"), ExtResource("94_error"), ExtResource("95_orchestrator")])
