[gd_resource type="Resource" script_class="EnemySpawn" load_steps=3 format=3]

[ext_resource type="Script" path="res://scripts/resources/enemy_spawn.gd" id="1_spawn"]
[ext_resource type="PackedScene" path="res://scenes/enemies/stale_replica.tscn" id="2_scene"]

[resource]
script = ExtResource("1_spawn")
; The other half of `load_balancer.tres`'s note, and the same correction. It was the *rarest* entry
; on the roster at 0.9 — below the Help Desk's filler — and missing from a quarter of floors.
weight = 1.7
min_difficulty = 2
scene = ExtResource("2_scene")
